<!DOCTYPE html>
<html lang="de" xmlns="http://www.w3.org/1999/xhtml" xmlns:t="http://tapestry.apache.org/schema/tapestry_5_4.xsd" xmlns:p="tapestry:parameter">
    <head>
        <meta charset="utf-8" />
        <title>${title}</title>
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <meta name="description" content="" />
        <meta name="author" content="" />
        <link rel="shortcut icon" href="${asset:context:/favicon.ico}" />
    </head>

    <body style="padding-top:0">
        <!-- INFO: conversation is the desired default strategy, but only if the conversation moderator works again for Tapestry 5.4.1 otherwise some persistent 
            page data will be deleted too often and too early <t:conversation.moderator warnBeforeHandler="warnBeforeHandler" endedHandler="endedHandler" /> -->

        <div class="container-fluid">
            <div class="col-md-6 lptop">
                <t:navbar />
            </div>

            <div class="col-md-30 lptop">
                <nav class="navbar navbar-default">
                    <div class="container-fluid">
                        <div class="navbar-header">
                            <t:eventlink event="toggleViewType" class="navbar-brand">
                                <t:if test="showVdrList()">
                                    Konfigurierte VDR:
                                    <p:else>
                                        Konfigurierte Sichten:
                                    </p:else>
                                </t:if>
                            </t:eventlink>
                        </div>

                        <ul class="nav navbar-nav">
                            <t:loop source="views" value="view">
                                <t:any element="li" class="prop:classForView">
                                    <t:eventlink event="selectView" context="view.displayName">
                                        ${view.displayName}
                                    </t:eventlink>
                                </t:any>
                            </t:loop>
                        </ul>
                    </div>
                </nav>

                <t:body />
            </div>
        </div>
    </body>
</html>
